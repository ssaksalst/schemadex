# LiteVault 交割文档

> 面向接手的人。README 讲「怎么用」，这份讲「为什么长这样、坑在哪、下一步做什么」。
> 最后更新：2026-08-06

---

## 1. 这是什么

本地 `.litematic` 蓝图库管理器。**只做两件事**（用户明确取舍，其余一律往后放）：

1. **不开游戏就能看到蓝图长什么样** —— 等距缩略图 + 3D 可旋转预览 + Y 轴逐层切片
2. **跨蓝图材料汇总** —— 多选蓝图，合并出一份「盒 / 组 / 个」的备货清单

**明确不做**：标签系统、全库检索、蓝图 diff、施工排程。这些讨论过，用户说「缓缓」。

### 为什么是这个定位

做之前调研过生电 mod 生态，结论是**绝大多数功能已经有人做了，别重复造**：

| 需求 | 已有方案 | 结论 |
| --- | --- | --- |
| 投影 / 材料清单 / 逐层 / Verifier | Litematica（1.21+ 由 sakura-ryoko 的 fork 维护，masa 本人已不更新） | 别做 |
| 材料清单 ↔ 仓库对账 | Better List for Litematica、Litematica Enderchest Materials | 别做 |
| Verifier 槽位级对比 | Technical Utilities (techutils) | 别做 |
| 农场速率 items/hour | Item Flow Monitor（Carpet counter 的客户端替代） | 别做 |
| 红石 tick 级时序调试 | Redstone Multimeter (RSMM) | 别做 |
| HUD 叠加（区块边界 / 生成球 / 光照） | MiniHUD + Servux | 别做 |
| 存档扫描找 stash | Minespector（CLI，单功能） | 部分空白 |
| 蓝图预览 | 只有在线单文件 viewer（shulkr / abfielder） | **本地库级管理没人做** |

唯一的空白就是**本地蓝图库管理**——用户硬盘上 2138 个 `.litematic`、171 MB、散在 6 个版本目录里，
文件名清一色 `iron_farm_v3_final_真final.litematic`，找一个蓝图只能靠回忆。

---

## 2. 当前状态

**能用。** 桌面应用跑起来能扫描、去重、看缩略图、开 3D 预览、逐层切片、多选算材料。

| 指标 | 数值 |
| --- | --- |
| 代码量 | 7397 行（Rust 5890 + TS 1507）<sup>※</sup> |
| 单元测试 | 59 个，全通过 |
| 全量对拍 | **2132 / 2138 与 Litematica 声明值完全一致，0 解析失败，0 新增不一致** |
| 扫描速度 | 2139 个文件索引 + 内容哈希 **1.2 秒** |
| 最大蓝图 | `流萤.litematic` 5.15 亿方块，缩略图 1.0s，单层切片 1.2s |
| 产物 | app 10.8 MB / cli 1.3 MB |

<sup>※</sup> 口径：`crates/*/src` 与 `src-tauri/src` 下的 `.rs` + `src` 下的 `.ts`/`.tsx`，
含注释和测试。这一行以前写的是 5797（Rust 4550），按同样口径量不出这个数，
应该是早期的旧数字一直没更新，别拿它跟现在的数做差。

对拍剩下的 6 个「不一致」是**蓝图作者把 Metadata 改成了梗数字**（`1919810` / `114514`），
不是解析 bug。这 6 个已按内容哈希写进 `verify` 的已知例外名单，
现在 `verify` 会自己判定通过与否并给出退出码，不用再靠人记住「2132」这个数。详见第 5 节。

---

## 3. 结构

```
C:\litevault\
├─ crates/
│  ├─ litematic/       .litematic 解析：流式 NBT、跨 long 位解包、材料映射
│  ├─ mcassets/        从客户端 jar 提取模型 / 材质图集 / 中文名
│  ├─ render/          等距缩略图、Y 轴切片、体素模型
│  └─ litevault-cli/   命令行，**也是正确性验证的主入口**
├─ src-tauri/          Tauri 后端命令
├─ src/                React 前端
└─ data/               colors-1.21.4.{json,png}，编进二进制
```

### 数据流

```
.litematic ──litematic──> Schematic { regions[].palette: BlockState{name, properties} }
                                              │
客户端 jar ──mcassets──> BlockAssets           │
                          ├ models[]           │
                          ├ blocks{} (规则)    │  resolve(name, props)
                          └ 图集 PNG           ▼
                                        Vec<ResolvedBox>   ← 一个方块 = 一组带六面贴图的长方体
                                              │
                          ┌───────────────────┼───────────────────┐
                       render 等距          render 切片        Tauri voxels
                          │                   │                   │
                       缩略图 PNG          切片 PNG          前端 three.js
```

### 各 crate 的职责边界

- **litematic** 只管解析，不知道材质和渲染的存在。`materials.rs` 里的方块→物品映射是手工表。
- **mcassets** 只管从 jar 提数据 + 按方块状态解析模型，不碰渲染。
- **render** 是纯 CPU 光栅化，输出 `RgbaImage`。3D 那条路只输出体素数据，渲染在前端。
- **litevault-cli** 不是玩具，是**验证入口**。每次改渲染都应该先用它出图核对。

---

## 4. 怎么构建

桌面应用**只能**这样构建：

```bash
npm install && npm run tauri build -- --no-bundle
```

> ⚠️ **别用 `cargo build -p litevault-app`。** 不经 Tauri CLI 编出来的是 dev 模式二进制，
> 前端资源没被内嵌，启动后去连 `devUrl`（localhost:1421），窗口里显示
> 「无法访问此页面 / localhost 拒绝连接」。
>
> 为此 `src-tauri` 已被排除出 workspace 的 `default-members`，根目录跑 `cargo build`
> 不会再误覆盖掉好的 exe。**判断手上的 exe 是哪种：生产构建里能搜到 `/assets/index-` 字符串。**
>
> 这个坑我踩过一次——收尾时跑了句 `cargo clean` + `cargo build` 检查警告，
> 把好的 exe 覆盖了，用户拿到手直接打不开。

命令行工具不受此限制：`cargo build --release`。

### 重新生成材质表

```bash
litevault colors "D:\新建文件夹\.minecraft\1.21.4\1.21.4.jar" data/colors-1.21.4.json
```

同时产出 `.json` 和 `.png`，两个都 `include_*!` 进二进制。

> **改了 `mcassets` 的提取逻辑就必须重新生成**，否则代码和内嵌数据会悄悄脱节：
> 表里存的是提取时算好的结果，光改代码不重新生成，跑的还是旧数据。
> 判断方法：重新生成一份到别处，跟 `data/` 里的比 `models` / `blocks` 两个字段，
> 逻辑没动的话应该逐字节相同。
>
> **jar 的位置**：这台机器上的 1.21.4 客户端在 `.minecraft\1.21.4\1.21.4.jar`，
> **不在** `versions\` 下面。中文名要顺着 jar 路径往上找 `.minecraft/assets/indexes`，
> 以前写死「往上两级」，对这个路径就找不着，静默退回全英文 ID（2636 条名字一条不剩，
> 而且不报错）。现在改成逐级往上找有 `assets/indexes` 的那层。
> 生成完先看一眼 `names` 的条数对不对。

**改了等距渲染的出图方式就把 `src-tauri/src/lib.rs` 里的 `RENDERER_VERSION` +1**
（当前 9）。缩略图磁盘缓存的键是蓝图内容哈希、不含渲染器版本，
不换目录用户会一直看到旧渲染器出的图。

缓存目录名和启动时要清理的旧目录名单**都由这一个常量算出来**，
以前是两处各写一遍（`THUMB_CACHE_DIR = "thumbs-v7"` 加一个手写的旧目录数组），
改一处漏一处就会留下几百 MB 垃圾或者让用户看到旧图。

> 注意判断标准是**等距那条路的输出变没变**，不是「有没有动 render crate」。
> 比如「玻璃不再遮挡邻居」那次改的是 `surface_voxels`（只喂 3D 视图），
> 等距渲染压根不查遮挡——它是把所有实心格子由远及近全画一遍——出图逐像素不变，
> 那次就没有 +1。白+1 的代价是让用户重新生成两千多张缩略图。

---

## 5. `.litematic` 格式：实测得出的硬约束

以下全部来自扫描 2138 个真实蓝图（171 MB，跨 MC 1.13 ~ 1.21.4）。**改解析器前先看这节。**

- **BlockStates 是跨 long 边界打包的**，不是 MC 1.16+ 区块那套「每个 long 内部不跨边界」。
  2708 个 region 全部满足 `longs == ceil(volume * bits / 64)`，无一满足不跨边界的算法。
  这是最容易翻车的一处，搞错了读出来全是乱的。
- **`Metadata` 里的统计值会被篡改**，一律自己算。实测 6 个文件把 `TotalVolume` 写成
  `1919810`、`TotalBlocks` 写成 `114514`，2 个把 `MinecraftDataVersion` 写成 `20060210`。
- **负 `Size` 是常态**（1757 / 2708 个 region），不是边缘情况。用 abs 取尺寸，
  `min_corner()` 里按符号还原起点。
- **单 region 体积可达 5.15 亿**（`流萤.litematic`，1424×601×602，解压后 515 MB）。
  索引阶段必须跳过 BlockStates；取单层切片必须**只解那一层**——
  先建完整 3D 网格再取一层要十几 GB，直接 OOM。
- **77 万个 TileEntity 没有 `id` 字段**（旧版 Litematica 不写）。
- **物品数量字段两种写法**：1.20.5 起 `Count`(byte) 改成了 `count`(int)，两种都得认。
- **73% 的蓝图 `Metadata.Name` 是 `"Unnamed"`**，文件名才是唯一有效信息。
- **64% 的文件是跨目录副本**（2138 文件 → 768 唯一蓝图）。去重按内容哈希（blake3），
  别按文件名或体积——实测有 2 对文件三项全同但字节不同。

### 正确性验证的标准答案

`Metadata.TotalBlocks` 是 Litematica 自己数出来的非空气方块数——**现成的标准答案**。
位解包、调色板顺序、空气判定错一处这个数就对不上。

```bash
litevault verify "D:\path\to\.minecraft\versions"
```

改动 `litematic` crate 之后**必须跑这个**。

它**自己会判定通过与否**：那 6 个梗数字文件按 blake3 内容哈希记在
`KNOWN_META_LIES` 名单里（4 个哈希覆盖 6 个文件——有一个在三个版本目录里各有副本），
输出分成「已知例外」和「**新出现的不一致**」两段，后者非空就以非零退出码失败。

> 以前这里只印一句「完全一致 2132 / 2138」，要靠人记住 2132 这个数才知道有没有退步——
> 悄悄掉到 2130 不会有任何人发现。名单化之后它才真正是个回归测试。
>
> 真遇到新出现的不一致时，它会直接把该填进名单的那一行打印出来。
> **但填之前先确认它确实是 Metadata 被篡改**：判据是「实算值和声明值的差说不通」，
> 比如那 6 个里有两个的实算体积比声明值还大——解析错了只会少数方块，不会凭空多出来。

---

## 6. 材质与模型：踩过的十一个坑

这部分反复返工了五轮，每一条都是真实踩过的。**核心原则：保留 MC 模型系统的原始结构，别压扁。**

蓝图调色板里每个方块都带着 `facing` / `half` / `axis` / `type` 等状态。
压成「一个方块名 → 一套贴图 + 一个包围盒」会导致所有活塞都朝上、所有活板门都在下半格、
楼梯只剩一整块、栅栏没有横杆。所以导出的是**模型库 + variant/multipart 规则**，
运行时用 `BlockAssets::resolve(name, props)` 按真实状态解析。

1. **键名不表示面。** 活塞的顶面贴图挂在 `platform` 键上，光看键名猜不到。
   必须从 model 的 `elements[].faces` 里读。
2. **模型不一定朝上画。** `template_piston` 是**朝北**画的，推头在模型的 `north` 面，
   靠 variant 的 `x` 旋转转到实际朝向。旋转方向由两个已知事实钉死并写成单元测试：
   活塞 `facing=up` 是 `x:270`（推头必须转到世界上方）、
   梯子 `facing=east` 是 `y:90`（朝东的梯子挂在西墙）。**别凭直觉改这两个映射表。**
3. **同尺寸的元素有先后。** 草方块有两个同为 16³ 的元素，第二个只是侧面的草色 overlay；
   `Iterator::max_by` 并列时返回**最后一个**，正好会选中 overlay，顶底面全丢。
4. **完全重叠的 element 只保留第一个。** MC 靠混合把高光层叠在本体上
   （红石粉 = `#line` 线路图案 + `#overlay` 高光）。我们不做混合，
   后画的会把前一层整个盖住，红石粉就成了一片纯红。
5. **variant 一个都匹配不上时要兜底到第一条。** 红石火把的 variant 键是
   `lit=true` / `lit=false`，状态里缺 `lit` 时两条都不匹配，方块整个消失、退化成纯色立方体。
6. **face 里的显式 `uv` 必须读。** 火把的元素在 x=7..9、y=0..10，按包围盒推顶面取样区是
   `[7,7,9,9]`，而模型里明写着 `[7,6,9,8]`——那才是火焰的位置。uv 属于面，旋转时跟着面走。
7. **面内纹理旋转要单独记。** 红石线、铁轨的走向靠 multipart 的 `y` 旋转实现，
   而这个旋转要作用在**顶面贴图内部**。只置换面不转面内 uv，南北向和东西向的红石线一模一样。
8. **箱子类方块实体的贴图在 `textures/entity/`。** 它们的 blockstate 指向 `builtin/entity`，
   模型里只有 `particle`（箱子的 particle 是橡木板，拿它贴满立方体箱子就成了一块木板）。
   按已知 UV 布局裁：箱盖顶 `(14,0) 14×14`、箱体正面 `(14,33) 14×10`。
9. **模型没声明的面就是不画，别拿别的面顶上。** 曾经有个
   `fill_missing_faces`：一个 element 上哪个面是空的，就拿同 element 上任意一个
   有效面的贴图填进去，理由是「免得出现空洞」。这个兜底凭空造出了两个 bug：
   - **红石线的游离小红点**：`redstone_dust_side` 只声明 up/down。顶出来的四个侧面
     按包围盒推 uv，落在贴图最底下那一行——正好是红的——于是 1/16 厚的薄片
     侧面上出现一个跟线本身分开的小红点。
   - **红石火把糊成一团**：1.21.4 的 `template_redstone_torch` 有 6 片零厚度、
     各自只声明一个面的辉光片。六面全顶满后每片都成了实心小方块，堆成一坨红。

   两个渲染器对**没贴图**的面本来就会退回平均色，根本不会漏出洞，所以这个兜底
   从一开始就是多余的。现在分成两个哨兵值：`NO_TILE`（这面要画但没贴图 → 平均色）
   和 `NO_FACE`（模型压根没声明 → 一个像素都不画）。
   **注意退回平均色是不够的**——那只是把「带纹理的错东西」换成「纯色的错东西」，
   红点照样在。等距、切片、3D 三条路都必须真的跳过。
11. **包围盒会伸出格子外，别夹回 0..16。** 活塞头的杆是
    `from[6,6,4] to[10,10,20]`——最后 4 格**故意**插进活塞本体那一格的凹槽里；
    墙上火把的 x 是 `-1..1`，扎进墙里半格。以前 bbox 是 `[u8;6]` 且两端各自
    clamp，杆就被截断在格子边界上：活塞「杆和本体是分开的」，中间空出 4/16 格，
    而且杆的断口那一面模型没声明、不画，看起来是个空心管。
    现在 bbox 是 `[i8;6]` 且不夹。算一遍就知道对不对——`piston[facing=east,
    extended=true]` 本体占 x 0..12（东侧留 4/16 凹槽），`piston_head[facing=east]`
    的杆是 x `-4..12`，换算到世界坐标正好从本体凹槽的起点接到头板的内面，严丝合缝。
    `litevault sample` 会把每个长方体的包围盒都列出来，伸出格子外的标上提示。
10. **「填满一格」和「挡得住」是两回事。** 玻璃、树叶都是严丝合缝的 16³ 完整立方体，
   但看得穿。遮挡剔除只看几何（`is_full_cube`）的话，玻璃罩里的整台机器
   会从 3D 视图里**整个消失**——而且这时候无论着色器怎么改都救不回来，
   数据根本没送到前端。所以判据必须是几何 + 透明度两条：`VoxelBlock::blocks_sight()`。
   透明度由 `Atlas` 建图集时扫一遍每个图块的 alpha 得出，分三档，见下。

### 透明度的三档，以及为什么 alphaTest 救不了染色玻璃

`render::Opacity` 把方块分成 `Opaque` / `Cutout` / `Translucent`，由图集 alpha 判定：

| 档 | 判据 | 例子 | 前端画法 |
| --- | --- | --- | --- |
| `Opaque` | 每个纹素都不透明 | 石头 | alphaTest 那一趟 |
| `Cutout` | 有全透明纹素、其余全不透 | 原版玻璃、树叶、铁栏杆 | 同上，剪掉即可 |
| `Translucent` | **存在半透明纹素** | 染色玻璃、淡化玻璃 | 单独一趟，真做 alpha 混合 |

**染色玻璃必须单独一趟。** 实测 `black_stained_glass` 的 256 个纹素 alpha 全在
102~163 之间——**一个全透明的都没有**。`alphaTest: 0.35` 对它完全无效：
没有任何纹素会被剪掉，材质又不是 `transparent`，alpha 于是被整个丢弃，
染色玻璃就渲染成一块实心色板。这就是「玻璃完全不透明」的直接成因。

半透明那一趟要 `transparent: true` + **`depthWrite: false`**。深度写入不关的话，
先画到的那片玻璃会把它背后的东西挡在深度测试外面，机器又变回看不见的了。
instanced 没法逐片排序，玻璃叠玻璃的地方会偏暗——但那恰好也是真玻璃叠起来的样子。

判定 `Cutout` 时要求全透纹素**至少占 1/64**，否则贴图边缘的一两个杂点
就会让整个方块不再遮挡，白白多出一堆体素。有单元测试钉着这个阈值。

> 等距渲染那条路**不受这一节影响**：它是把所有实心格子由远及近全画一遍、
> 逐纹素 alpha 混合，压根不做遮挡剔除，玻璃一直是对的。
> 这个 bug 从头到尾只存在于 3D 视图。定位时先分清是哪条路，别一起改。

### 透明处理

**图集保留 alpha，透明留到绘制时混合。** 曾经在打包时就合成掉，结果红石线底下多出
一层不该有的暗红方底。等距渲染是画家算法由远及近画的，「已有像素」就是这个方块背后的东西，
直接 alpha 混合即可；背后还是空画布时才退回压暗四成的平均色，
免得漏斗顶那种镂空贴图在方块上开个洞、直接透出背景。

### 3D 视图的两个坑

- **图集采样必须翻转 v。** WebGL 纹理坐标 `v=0` 在**底部**，而图集 0 号图块在图片**左上角**、
  行号自上而下数。直接拿 `row/N` 当 v 用等于把整张图集上下翻转，
  **每个方块都会采到镜像行的图块**——表现就是「所有方块材质全错」。
  正确写法 `v = 1 - (row + 1 - uv.y) / N`。
- **必须传满六个面。** 只给「顶/侧/底」三个槽，四个侧面会共用同一张贴图，
  朝北的粘性活塞四面都是黏液绿。六面各要 4 个 uv 分量加 1 个旋转 = 30 个 instanced 属性，
  顶点属性槽不够；uv 分量是 0..16 整数、旋转 0..3，**用 17 进制打包成一个 float**
  （最大 334083，float32 的 24 位尾数能精确表示）。

### 中文名

**jar 里只有 `en_us.json`。** 其它语言由启动器单独下载，存在
`.minecraft/assets/objects/<hash 前两位>/<hash>`，由 `assets/indexes/*.json` 索引。
提取时顺着 jar 路径往上两级找到 `.minecraft` 去捞，捞不到就退回英文 ID。当前 2636 条。

---

## 7. 怎么验证渲染改动

**别靠肉眼看整机图猜。** 渲染出来的机器上分不清哪块是什么方块，我在这上面反复误判过。

### 等距 / 切片：用对照表

```bash
litevault sample data/colors-1.21.4.json out.png \
  "oak_stairs[facing=east,half=bottom]" "redstone_wire[power=0,north=side,south=side]" torch
```

每个方块画一个孤立立方体，**有已知答案才判断得了对错**。语法支持带方块状态。

### 3D 着色器：程序化比对，不看图

前端着色器改动**必须**这样验，我吃过亏（v 翻转那次没验就交付，用户直接说「所有方块材质全错」）。

脚手架现在是**常驻**的：`tools/rendercheck.html`。以前每次改着色器都临时写一个再删掉，
下一次又从零搭一遍——留着的成本只有一个文件，重搭的成本是每次都可能搭得不一样。

1. 在 `C:\lumina-project\.claude\launch.json` 建一条 `litevault-web`
   —— 注意 launch.json 要放在 **cwd** 下，不是项目下，所以它没法跟项目一起提交。
   实测能用的配置（`cwd` 字段会被拒，必须直接指 vite 的可执行文件、根目录走位置参数）：

   ```json
   {
     "version": "0.0.1",
     "configurations": [{
       "name": "litevault-web",
       "runtimeExecutable": "C:\\litevault\\node_modules\\.bin\\vite.cmd",
       "runtimeArgs": ["C:\\litevault", "--config", "C:\\litevault\\vite.config.ts",
                       "--port", "1421", "--strictPort"],
       "port": 1421
     }]
   }
   ```

2. `preview_start` 起服务，`navigate` 到 `/tools/rendercheck.html`
3. 用 `javascript_tool` 读 `window.__result`，看 `pass` 和 `checks`

它渲染一个正交相机正对的合成场景（石头 / 石头+染色玻璃 / 石头+普通玻璃 /
单独的玻璃 / 石头+一块朝相机那面标了 `NO_FACE` 的方块），
`readPixels` 取几个已知位置的像素，逐通道和 alpha 混合公式的期望值比。当前 5 项检查。
**页面里的着色器代码必须和 `Viewer3D.tsx` 的 `patchShader` 逐字一致**，
否则验的就不是线上那份——加新检查时先核对这一点。

> **对照组不能省。** 只验「玻璃后面的石头变蓝了」是不够的，那也可能是玻璃自己的颜色。
> 得再画一块背后什么都没有的玻璃，证明两者不是同一个颜色。
> 而且从 canvas 读回来的 RGB 是**预乘过 alpha** 的，拿它直接跟混合结果比大小会得出
> 错误结论——必须先除回去还原源色。我第一版检查就是栽在这里，报了个假的 fail。

> Browser pane 直接 `navigate` 到 `localhost` 会被策略拦，必须走 `preview_start`。
> Browser pane 没显示时 `screenshot` 会超时，所以用 `javascript_tool` 读数据而不是截图。

---

## 8. 环境坑（这台机器特有）

- **必须在纯 ASCII 路径下构建。** 用户名含中文，Rust 工具链须为 MSVC。
  项目在 `C:\litevault`，`CARGO_HOME=C:\cargo_home`。
- **PowerShell 5.1 把无 BOM 的 UTF-8 按 GBK 读。** `.ps1` 脚本里**别写中文注释**——
  中文可能被错解出游离引号，把后面的 here-string 整段吞掉。（写文件用
  `[System.IO.File]::WriteAllText($p, $t, (New-Object System.Text.UTF8Encoding($false)))`）
- **Bash 的 heredoc 不可靠。** 用 `python - <<'PY'` 内联脚本经常报
  `unexpected EOF while looking for matching`。**改成先把 .py 写到 scratchpad 再执行。**
- **`python3` 指向 Windows Store 占位程序**，会静默失败。用 `python`。
- **PowerShell 进程默认 DPI 不感知。** 这台机器真实分辨率 **2520×1680**，
  不调 `SetProcessDPIAware()` 的话 `Screen.Bounds` 和 `MoveWindow` 会被虚拟化成 1680×1120，
  窗口一半跑到屏幕外。
- **用户的蓝图目录**：`D:\新建文件夹\.minecraft`（PCL 启动器，**版本隔离**，
  蓝图散在 6 个 `versions/*/schematics/` 下）。默认的 `%APPDATA%\.minecraft` 是空的。
- **1.21.4 客户端 jar 在 `.minecraft\1.21.4\1.21.4.jar`**，不在 `versions\` 下面
  （`versions\` 里最新的只到 1.21.1-NeoForge）。重新生成材质表要用它，见第 4 节。
- **别长时间抢用户前台。** 自动化驱动 GUI 时截到过用户的浏览器窗口——他在用电脑。
  能用 CLI 验证的就别开 GUI。

---

## 9. 已知缺口 / 下一步

### 先划清这条线：验收标准是「认得出」，不是「像游戏」

**这是个缩略图 / 预览工具，不是 MC 建模器。**（2026-08-06 用户明确说的，别再滑回去。）

渲染问题值不值得修，判据是**它妨不妨碍在预览尺度上认出这是什么机器**：

| 修了 | 为什么值得 |
| --- | --- |
| 大蓝图整张变色块 | 方块全认不出来，预览直接失去意义 |
| 红石线的游离小红点 | 电路走向读错 |
| 红石火把糊成一团红 | 分不清是火把还是别的红色方块 |
| 活塞杆和本体断开 | 看起来像蓝图里缺了方块 |
| 玻璃不透明 | 罩里的整台机器看不见 |

| 不修 | 为什么不值得 |
| --- | --- |
| 墙上火把差 22.5° | 一眼就知道那是火把，斜不斜不影响读图 |
| 拉杆手柄是直立的 | 同上 |
| 等距图不能转视角 | 想转就去 3D 视图 |

**遇到新的渲染瑕疵，先问「不修的话会认错方块吗」。** 答案是「不会，只是不够精确」
就别修——那个方向没有底，MC 的模型系统能一直挖下去。

### 不做：element 级旋转
拉杆的手柄、爬坡铁轨、墙上的火把都带
`"rotation": {"origin":[..], "axis":"z", "angle":-22.5}`，完全没处理。
现在拉杆是「圆石底座 + 直立木柄」，墙上火把是直插墙里的竖柱而不是斜的。

等距那条路要做这个得写通用三角形光栅器（现在只画轴对齐长方体），是另一个量级的改动；
3D 那边虽然便宜（instance 本来就带变换矩阵），但两边不一致又是新的麻烦。

**用户已明确说不做**，理由就是上面那条线：认得出就够了。
除非哪天出现「因为没旋转导致方块认错」的具体例子，否则别翻这个案。

### 其余按判断排序：

### 中：材料清单的方块→物品映射是手工表
`crates/litematic/src/materials.rs`。离线拿不到游戏的 `getCloneItemStack`，
只能手工维护。已知不精确的项（如蜡烛蛋糕只算蛋糕）会标 `~` 提示复核。
理论上可以改成从 `mcassets` 的模型数据反推，但收益不大。

### 中：材料清单没跟 `mcassets` 打通
材料清单走的还是「方块名 → 物品」，没用上按状态解析的结果。
双层台阶 ×2 这类是手工特判的。合并之后会更准。

### ~~低：3D 视图的实例数没有上限保护~~（已做）
上限现在卡在 instance 上（`MAX_INSTANCES = 900_000`），不再卡体素数。
一个方块可能展开成多个长方体（栅栏 5 个），实测膨胀 1.9~2.1×，
以前只卡体素数等于把上限放宽了一倍。`litevault voxels` 会打印
「instance 数 …，膨胀 …×」，口径和前端数 instance 的方式一致。

### 低：等距图不支持透视/旋转
固定 2:1 等距、固定视角。想转就去 3D 视图。

### 降采样的体素为什么要贴图（别改回去）

`target_grid` 默认 128，蓝图最长边超过它就降采样，一个体素代表 scale³ 个方块，
按完整立方体画。**这个立方体六面贴的是该方块的代表图块**（`VoxelBlock::repr`），
不是平均色。

以前这里退回平均色，后果是「**有些蓝图有材质、有些整张都是色块**」——
分界线是蓝图尺寸，用户完全看不出规律，只会觉得渲染时好时坏。
真要省，判据应该是「一个方块在屏幕上有几个像素」（`min_texture_px`，已经有了），
而不是「有没有降采样」。这两件事是独立的，别再混在一起。

### 没做的功能（用户明确说「缓缓」）
标签、全库检索、蓝图 diff（schematic ↔ schematic）、施工排程、配套 Fabric mod。

### 真正该往下做的是材料清单那两条

**三条剩余项里，验证强度是不对称的。** 解析侧有 `verify` 这个覆盖全量真实数据的
oracle；渲染侧有 `sample` 对照表和 `tools/rendercheck.html`；
**材料清单侧什么都没有**——靠 `materials.rs` 一张手工表，已知不精确的项只标个 `~`。

而材料汇总是第 1 节写明的两大支柱之一，用户拿它去实际备货，数字错了是真金白银的损失。
渲染那边现在已经过了「认得出」这条线，边际收益很低了；材料清单还没有任何自动校验手段。
**下一个人要挑活干，从这两条里挑，别再回去抠模型。**

---

## 10. 给接手的人的五条建议

1. **改 `litematic` 就跑 `verify`，改渲染就出 `sample` 对照表，改着色器就跑
   `tools/rendercheck.html`。** 这个项目返工了五轮，每一轮都是因为
   「看着差不多」就交付了。三个入口都是现成的，跑一次都不到一分钟。
2. **第 6 节那十一条坑不是经验之谈，是血泪。** 每一条都对应一次用户说「不对」。
   动模型解析前把那节读完。
   其中第 9 条最值得记：**别给渲染器加「兜底」**。
   `fill_missing_faces` 就是个好心的兜底，结果自己造出了两个 bug，
   而且因为它「看起来很合理」，藏了很久没人怀疑。
   拿不准某个面该不该画的时候，去翻 jar 里的模型 JSON，别猜。
3. **旋转映射表（`model.rs` 的 `perm_x` / `perm_y`）别凭直觉改。** 它们由活塞和梯子
   两个已知事实反推而来，有单元测试钉着。改之前先想清楚你的基准是什么。
4. **这是缩略图工具，不是 MC 建模器。** 渲染上的完美主义在这个项目里是负债：
   MC 的模型系统能一直挖下去（element 旋转、AO、tintindex、动画贴图…），
   而用户要的只是「不开游戏能认出这是什么机器」。
   每条渲染改动先过第 9 节开头那条线：**不修的话会认错方块吗？**
5. **先分清 bug 在哪条路上。** 等距缩略图、Y 轴切片、3D 视图三条路共用解析和图集，
   但**渲染逻辑是各自独立的**——等距和切片是 Rust 里的 CPU 光栅化，3D 是前端
   WebGL 着色器，两边的遮挡、混合、uv 约定都不一样。
   「玻璃不透明」那次只坏在 3D 一条路上，等距一直是对的；
   一开始要是默认三条路一起坏，就会去改本来没问题的代码。
